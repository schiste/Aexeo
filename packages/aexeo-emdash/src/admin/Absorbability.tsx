// Absorbability pillar admin page.
//
// Layer 3 of the four-layer GEO model: does the answer actually use
// the page's content? Cite-ready evidence, markdown mirrors, llms.txt.
// Findings whose rule's primary layer is `absorbability` land here.

import * as React from "react";
import { PillarView } from "./PillarView.js";

export function Absorbability(): React.JSX.Element {
  return <PillarView layer="absorbability" />;
}
